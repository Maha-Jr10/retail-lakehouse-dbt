sources:
  - name: source
    database: "{{ env_var('DBT_SOURCE_CATALOG', 'dbt_tuto_dev') }}"
    schema: source
    tables:
      - name: dim_product
      - name: dim_customer
      - name: dim_date
      - name: dim_store
      - name: fact_sales
      - name: fact_returns